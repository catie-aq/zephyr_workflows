name: 'Generate Zephyr Manifest'
description: 'Generates the west.yml file'
inputs:
  zephyr_revision:
    required: true
    type: string
  sixtron_revision:
    required: true
    type: string
  repository_revision:
    required: true
    type: string
  repo_board:
    required: true
    type: string
runs:
  using: "composite"
  steps:
    - name: Create workdir/west.yml
      shell: bash
      run: |
        mkdir -p workdir
        touch workdir/west.yml

    - name: Replace underscores with hyphens
      id: replace-characters
      shell: bash
      run: |
        REPO_BOARD=${{ inputs.repo_board }}
        REPO_BOARD_WITH_HYPHENS=$(echo $REPO_BOARD | tr '_' '-')
        echo "repo_board: $REPO_BOARD_WITH_HYPHENS" >> $GITHUB_OUTPUT
    
    - name: Generate YAML file
      shell: bash
      run: |
        cat << EOF > workdir/west.yml
        manifest:
          remotes:
            - name: zephyrproject-rtos
              url-base: https://github.com/zephyrproject-rtos
            - name: catie-6tron
              url-base: https://github.com/catie-aq
          projects:
            - name: zephyr
              remote: zephyrproject-rtos
              revision: ${{ inputs.zephyr_revision }}
              import: true
            - name: ${{ github.event.repository.name }}
              remote: catie-6tron
              revision: ${{ inputs.repository_revision }}
            - name: 6tron-manifest
              remote: catie-6tron
              repo-path: zephyr_6tron-manifest
              revision: ${{ inputs.sixtron_revision }}
              path: 6tron/6tron-manifest
              import: 
                name-allowlist:
                  - 6tron-connector
                  - ${{ steps.replace-characters.outputs.repo_board }}
        EOF
